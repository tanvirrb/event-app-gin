version: 2
linters:
  enable-all: true

formatters:
  default: []

run:
  deadline: 5m
  tests: true
  skip-dirs:
    - vendor
    - third_party
    - tmp
  out-format: colored-line-number

issues:
  max-issues-per-linter: 0
  max-same-issues: 0 